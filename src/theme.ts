import { createTheme } from '@mui/material/styles';

export const muiTheme = createTheme({
  palette: {
    primary: { main: '#1EA4CE' },
    secondary: { main: '#147594' },
  },
});

export const theme = {
  color: {
    primary: '#1EA4CE',
    primaryDark: '#147594',
    primaryLight: '#F2F0FD',
    white: '#fff',
  },
};
